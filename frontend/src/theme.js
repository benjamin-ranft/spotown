import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#00B17C",
      light: "#FFF",
    },

    secondary: {
      main: "#EAEBEA",
      dark: "#001514",
    },
  },
});

export default theme;
